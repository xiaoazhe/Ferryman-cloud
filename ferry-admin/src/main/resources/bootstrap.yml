spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        serviceName: Ferry-admin    # 注册到consul的服务名称
    config:
      discovery:
         enablead: true
         serviceId: Ferry-config
      name: admin
      profile: dev
      label: master
      uri: http://127.0.0.1:8020