spring:
  application:
    name: Ferry-webConsumer
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        serviceName: Ferry-webConsumer
    config:
      discovery:
         enablead: true
         serviceId: Ferry-config
      name: webConsumer
      profile: dev
      label: master
      uri: http://127.0.0.1:8020